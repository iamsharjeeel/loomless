# Supabase — required to wire the data-access layer (lib/data.ts) to real data.
# When unset, the app renders empty/pending states (Phase 1 UI pass).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
